const userApp  = require('../../src/app/application/user')

describe('auth', ()=>{
    it('should create new user', async ()=>{

        /*const repository = AppDataSource.getRepository(User)

        const user = await repository.save({
            "email":"claudio.ssilva@gmail.com",
            "nome":"Claudio",
            "sobrenome":"Santos",
            "password":"123455"
        })

        console.log( user )

        expect( user.email ).toBe("claudio.ssilva@gmail.com")*/

        expect( 8 ).toBe(8)

    })
})

